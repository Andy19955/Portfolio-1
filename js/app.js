import { toggleNavigation } from "./toggleNavbar.js";

function router() {
  const { pathname } = location;

  switch (pathname) {
    case "/":
    case "/index.html":
    case "/index":
  }
}

router();
toggleNavigation();
